import React, { useState } from 'react'


const History = () => {
  // const [number, setNumber] = useState(0);
  // console.log('rendering')
  return (
    <div>
          <h1>History</h1>
          {/* <p>{`The total count is ${number}`}</p> */}
          {/* <button onClick={() => setNumber((prevState) => prevState + 1)}>Add</button> */}
    </div>
  )
}

export default History